import {
    ADD
} from '../actions/actionTypes';

const cartReducer = (state={}, action) => {
    switch(action.type) {
        case ADD:
            return state
    }
}

export default cartReducer;